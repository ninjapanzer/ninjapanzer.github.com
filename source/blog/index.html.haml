---
pageable: true
per_page: 1
---

.blog
  - page_articles.each_with_index do |article, i|
    %h4.body--header
      %span= article.date.strftime('%B %e')
      = link_to article.title, article
      .abstract.body--content= article.data.abstract
  - if paginate
    .next-prev
      - if num_pages > 1 && prev_page
        = link_to '<<==', prev_page
      Page #{page_number} of #{num_pages}
      - if next_page
        = link_to '==>>', next_page
